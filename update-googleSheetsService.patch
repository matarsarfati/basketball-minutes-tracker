--- src/services/googleSheetsService.js
+++ src/services/googleSheetsService.js
@@ -69,14 +69,14 @@
         if (morningRow[c]) {
           const morningEvent = parseCellText(morningRow[c], 'AM');
           if (morningEvent) {
-            parsedEvents.push({ ...morningEvent, date: dateStr, created: new Date().toISOString() });
+            parsedEvents.push({ ...morningEvent, date: dateStr, created: new Date().toISOString(), sheetRef: `Sheet1!${String.fromCharCode(65 + c)}${i + 4}` });
           }
         }
         
         // Evening
         if (eveningRow[c]) {
           const eveningEvent = parseCellText(eveningRow[c], 'PM');
           if (eveningEvent) {
-            parsedEvents.push({ ...eveningEvent, date: dateStr, created: new Date().toISOString() });
+            parsedEvents.push({ ...eveningEvent, date: dateStr, created: new Date().toISOString(), sheetRef: `Sheet1!${String.fromCharCode(65 + c)}${i + 5}` });
           }
         }
       }
